# Use the skeleton

This repository currently provides a minimal placeholder binary and the surrounding governance skeleton.

Typical validation path:

```bash
win-ctl-cli --version
win-ctl-cli
pnpm run docs:build
```
